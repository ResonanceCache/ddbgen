# Access patterns

<!-- Generated by ddbgen. DO NOT EDIT. -->

This document is emitted by `ddbgen docs` from the same schema parse
that generates the client and the infrastructure, so it is always
current: if a pattern changes, this file changes in the same commit.

## Table `app`

| Pattern | Index | Key condition | Returns | Generated method |
|---|---|---|---|---|
| OrdersByStatus | GSI1 | pk = "STATUS#{Status:upper}" — refinable via UpdatedAfter / UpdatedBefore / UpdatedBetween | []Order (All iterator / Page) | `OrdersByStatus(status)` |
| OrdersByTenant | main | pk = "TENANT#{TenantID}" AND begins_with(sk, "ORDER#") — refinable via CreatedAfter / CreatedBefore / CreatedBetween | []Order (All iterator / Page) | `OrdersByTenant(tenantID)` |
| PaymentsByOrder | main | pk = "TENANT#{TenantID}" AND begins_with(sk, "PAY#{OrderID}#") | []Payment (All iterator / Page) | `PaymentsByOrder(tenantID, orderID)` |
| TenantPartition | main | pk = "TENANT#{TenantID}" | TenantCollection{Orders, Payments, Tenants, Unknown} | `TenantPartition(tenantID).Collect(ctx)` |
